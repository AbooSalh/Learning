import { describe, test, it, expect } from "vitest";
 import { max } from "../src/intro";
describe("max", () => {
  it("should return the maximum", () => {
    expect(max(1, 2)).toBe(2);
  });
  it("should return the second maximum", () => {
    expect(max(1, 2)).toBe(2);
  });
});
describe("fizzBuzz", () => {
    it("should return fizzBuzz ", () => {})
})