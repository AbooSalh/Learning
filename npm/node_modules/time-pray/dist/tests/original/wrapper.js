"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.OriginalPraytimes = void 0;
const PrayTimes_1 = __importDefault(require("./PrayTimes"));
function OriginalPraytimes(inputs) {
    const p = new PrayTimes_1.default();
    if (inputs.method)
        p.setMethod(inputs.method);
    if (inputs.params)
        p.adjust(inputs.params);
    const res = p.getTimes(inputs.date, [
        inputs.location.latitude,
        inputs.location.longitude,
        inputs.location.elevation,
    ], 0, 0, "Float");
    return Object.fromEntries(Object.entries(res).map(([k, v]) => [
        k,
        v != "-----"
            ? convertToDate(inputs.date, v)
            : new Date(NaN),
    ]));
}
exports.OriginalPraytimes = OriginalPraytimes;
function convertToDate(dateParts, hours) {
    return new Date(Date.UTC(dateParts[0], dateParts[1] - 1, dateParts[2]) +
        hours * 3600 * 1000);
}
//# sourceMappingURL=wrapper.js.map